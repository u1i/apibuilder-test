'use strict';

exports = module.exports = {
	stringify(req, cb) {
		cb.next(null, JSON.stringify(req.params.value, null, req.params.space || 0));
	},
	parse(req, cb) {
		try {
			cb.next(null, JSON.parse(req.params.json));
		} catch (e) {
			cb.error(null, e);
		}
	}
};